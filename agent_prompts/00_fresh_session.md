# Fresh agent session prompt

Read `AGENTS.md`, `FAST_ADVANCE.md`, and `AGENT_START_HERE.md`. Run `python scripts\verify.py` when the environment is ready, then run `python scripts\task.py status`. Inspect `WORKLOG.md` and the active plan.

Advance the highest-leverage safe local implementation slice. Bottom-up is the default, but do not stall on rigid milestone order when a bounded top-down spike or parallel fixture-backed task would remove uncertainty faster. Preserve local-only scope, Postgres-first storage, raw evidence lineage, offline default verification, and no trading/order-placement.

Update `WORKLOG.md` with changed files, checks run, blocker status, and the next action.


When architecture, orchestration, data semantics, or next-slice selection is unclear, use an orthogonal approach and a compact Talmudic debate. Record only the consensus and executable validation target; do not create ceremony.


Decision method reference: `docs/governance/12_decision_methods.md`.
