# Verification pass prompt

Run the relevant narrow checks and then `python scripts\verify.py`. Inspect changed files against `AGENTS.md`, the active plan, and applicable ADRs. Report any mismatch between implementation, tests, docs, and product intent. Fix only small obvious issues; otherwise update `WORKLOG.md` with blockers and next steps.
