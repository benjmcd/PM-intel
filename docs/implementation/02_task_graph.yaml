version: 2
execution_model: adaptive_bottom_up
primary_environment: windows_local_directory
primary_storage: postgres
required_default_gate: python scripts\verify.py
fast_advance_contract: FAST_ADVANCE.md
adaptive_map: docs/implementation/06_adaptive_milestone_map.md
current_posture:
  summary: implemented local core is handoff-ready with offline, fixture, DB-gated,
    and bounded live evidence; this is not final long-term completion.
  source_of_truth: WORKLOG.md plus current executable verification; this task graph
    is the status surface consumed by scripts/repo_status.py.
  constraints_intact:
  - local_only_scope
  - postgres_first_storage
  - raw_evidence_lineage_before_derived_records
  - no_trading_or_order_placement
  - default_tests_make_no_live_api_calls
  verified_proof:
  - Strict Polymarket live soak passed on 2026-06-18 with raw_events=11643,
    normalized_trades=781, alerts=10, unresolved_dead_letters=0,
    open_data_quality_incidents=0, raw_evidence_duration_minutes=68.9.
  - Strict Kalshi-required live soak passed on 2026-06-18 with kalshi raw_events=1144,
    kalshi normalized_trades=1144, kalshi raw_evidence_duration_minutes=60.862,
    unresolved_dead_letters=0, open_data_quality_incidents=0, and copied-ticker
    sync/watch proven through pmfi markets sync-one.
  - Tier-1 alert-quality review recorded 23 local Postgres review rows on 2026-06-18;
    reviewed volume_spike_v1 low_notional+thin_baseline alerts were labeled noise
    with category low_notional_thin_baseline.
  - Tier-1 review of the remaining market_relative_large_trade_v1 alert recorded
    one true-positive review row on 2026-06-18; the trade was the local maximum
    for its market window, above the post-window p99.5, with the sparse-baseline
    caveat retained in the review category.
  - volume_spike_v1 now has a configurable min_trade_usd=500 floor backed by reviewed
    noise evidence and fixture tests, suppressing sub-$500 spike-only alerts while
    preserving higher-notional spike detection.
  - Read-only 24h DB replay with current post-tuning rules processed 12797 raw
    events, produced 1935 normalized results and 30 in-memory alerts, and emitted
    zero volume_spike_v1 alerts below the configured 500 USD floor.
  - >-
    Publish-readiness validation passed after fetching origin on 2026-06-18:
    branch main was clean, ahead 52 and behind 0 against origin/main, with no
    attribution/generated-footer hits.
  - >-
    Publication completed on 2026-06-18: git push origin main succeeded, a fresh
    fetch confirmed local HEAD and origin/main matched, and the worktree was clean
    with no ahead/behind delta.
  - >-
    Fresh post-publication bounded live ingest passed on 2026-06-18 with raw_events=2021,
    normalized_trades=290, alerts=0, unresolved_dead_letters=0,
    open_data_quality_incidents=0, raw_evidence_duration_minutes=9.965, and
    both required venues present: kalshi raw_events=266 normalized_trades=266
    duration_minutes=8.804; polymarket raw_events=1755 normalized_trades=24
    duration_minutes=9.965.
  - >-
    Dashboard alert hardening now surfaces deterministic triage flags in the
    read-only /api/alerts payload and browser alerts table using the shared
    pmfi.alert_triage helper, aligning dashboard review context with pmfi alerts
    list and pmfi report.
  - >-
    Exact-run soak validation now supports explicit --since and --until bounds,
    fails closed for malformed, naive, future, or inverted timestamps, and routes
    through both pmfi soak and python scripts\task.py soak.
  - >-
    Fresh exact bounded current-traffic soak passed on 2026-06-18 for the
    completed run from 2026-06-18T12:45:04.533894+00:00 through
    2026-06-18T13:58:12+00:00 with raw_events=15711,
    normalized_trades=723, alerts=0, unresolved_dead_letters=0,
    open_data_quality_incidents=0, raw_evidence_duration_minutes=69.984, and
    both required venues present: kalshi raw_events=167 normalized_trades=167
    duration_minutes=69.939; polymarket raw_events=15544
    normalized_trades=556 duration_minutes=69.984.
  - >-
    Dashboard alert filtering is now read-only and operator-facing: /api/alerts
    accepts review_state, latest review_label, and repeated or comma-separated
    triage_flag filters, rejects invalid/conflicting filters with HTTP 400, and
    the browser dashboard exposes matching controls while GET filtering remains
    non-mutating and separate from the review POST path.
  - >-
    Dashboard alert review writes are implemented as a localhost-only append-only
    POST /api/alerts/{alert_id}/review path. It accepts label tp/fp/noise plus
    optional string category, notes, and reviewed_by fields, resolves full UUIDs
    or existing short alert prefixes through the shared alert repository helper,
    returns inserted review metadata, and leaves GET/read endpoints non-mutating.
  - >-
    Local dashboard review endpoint smoke passed against local Postgres on
    2026-06-18: malformed JSON returned HTTP 400, invalid labels returned HTTP
    400, missing alert IDs returned HTTP 404, a short-prefix POST inserted one
    append-only tp review row, and GET /api/alerts with reviewed/tp filters
    returned the inserted latest-review state before synthetic cleanup.
  - >-
    Headless and headed Chrome smoke passed on 2026-06-18 against the localhost
    dashboard review controls using the installed Chrome channel. Each browser
    submitted one synthetic unreviewed alert through the in-page review form,
    refreshed the row to its latest-review label/category, detected zero
    table-cell overlaps at a 1440x950 viewport, and synthetic alerts/markets
    were cleaned up afterward.
  - >-
    Dashboard review-write hardening now rejects non-application/json POSTs
    before DB access, rejects foreign Origin/Referer headers with HTTP 403,
    validates route-level 400/404/200 status mapping in default tests, and escapes
    quotes in dashboard-rendered review metadata to prevent attribute injection;
    headed/headless browser smoke preserved quoted review notes as inert text.
  - >-
    Dashboard-origin review cross-surface smoke passed on 2026-06-18: one
    synthetic alert was reviewed through the dashboard POST path, pmfi report
    surfaced the false-positive category, pmfi alerts fp-rate reported Reviewed=1
    and FP=1 for the unique rule, pmfi alerts list --reviewed --review-label fp
    returned the same alert/review truth, and synthetic rows were cleaned up.
  - >-
    Local review-packet export is implemented through pmfi alerts review-packet.
    It writes ignored JSON artifacts under reports/review-packets by default,
    constrains custom outputs to that ignored directory without overwriting
    existing files, uses latest-review authority, supports
    since/rule/review-label/category/limit filters, and includes reviewed cohort
    totals, latest review metadata, evidence summaries, deterministic triage
    flags, lineage IDs, and window report context.
  - >-
    Review-packet DB smoke passed on 2026-06-18: pmfi alerts review-packet
    --since 24h --limit 5 wrote reports\review-packets\smoke.json with 24
    reviewed alerts in the cohort, by-label counts noise=23 and tp=1, by-rule
    counts volume_spike_v1=23 and market_relative_large_trade_v1=1, raw_events=30529,
    normalized_trades=2948, unresolved_dead_letters=0, and open_data_quality_incidents=0.
  - >-
    Packet-backed calibration decision recorded on 2026-06-18 in
    docs/product/03_calibration.md: the reviewed packet does not justify another
    threshold change, the existing volume_spike_v1.min_trade_usd=500 floor remains
    the active noise control, market_relative_large_trade_v1 remains unchanged,
    and the next proof target is fresh post-calibration runtime evidence.
  - >-
    Fresh post-calibration exact bounded live/soak proof passed on 2026-06-18
    for the completed run from 2026-06-18T15:38:25.467800+00:00 through
    2026-06-18T15:48:27.944875+00:00 with raw_events=3445,
    normalized_trades=343, alerts=1, unresolved_dead_letters=0,
    open_data_quality_incidents=0, raw_evidence_duration_minutes=9.982, and
    both required venues present: kalshi raw_events=305 normalized_trades=305
    duration_minutes=9.859; polymarket raw_events=3140 normalized_trades=38
    duration_minutes=9.982. The run produced Kalshi volume_spike_v1 evidence
    above the configured floor, with
    this_trade_usd=1695.75, min_trade_usd=500, spike_multiplier=60.78, and
    baseline_trades=20.
  - >-
    Fresh post-calibration alert review closeout recorded f5f72655 as a local
    tp review row on 2026-06-18 with category post_calibration_volume_spike:
    dry-run resolved the intended alert without writing, the append-only review
    write succeeded, pmfi alerts list returned review_label=tp with raw_event_id=34755
    and trade_id=de2d95eb-ea8d-4c0e-8f0c-0c5e1a48e2f6, pmfi alerts fp-rate
    --since 30m reported Reviewed=1, FP=0, TP=1, Noise=0, pmfi report --since
    30m reported review_queue.total=0 and reviewed_total=1, and
    pmfi alerts review-packet --since 30m --review-label tp wrote an ignored
    local packet with alerts=1, raw_events=3445, normalized_trades=343,
    unresolved_dead_letters=0, and open_data_quality_incidents=0.
  - >-
    Second post-calibration exact bounded live/soak sample passed on
    2026-06-18 for the completed run from 2026-06-18T15:59:55.470717+00:00
    through 2026-06-18T16:09:57.919227+00:00 with raw_events=4075,
    normalized_trades=206, alerts=5, unresolved_dead_letters=0,
    open_data_quality_incidents=0, raw_evidence_duration_minutes=9.985, and
    both required venues present: kalshi raw_events=178 normalized_trades=178
    duration_minutes=9.607; polymarket raw_events=3897 normalized_trades=28
    duration_minutes=9.985.
  - >-
    The five-alert post-calibration sample was fully reviewed on 2026-06-18:
    pmfi alerts fp-rate --since 20m reported Reviewed=5, FP=1, TP=3, Noise=1;
    pmfi report --since 20m reported review_queue.total=0, reviewed_total=5,
    false_positive_categories directional_outcome_mismatch=1, and no unresolved
    dead letters or open data-quality incidents. The reviewed rows were two
    clean directional_cluster_v1 true positives, one large_trade_absolute_v1
    true positive with payout-notional/low-capital caveat, one
    directional_cluster_v1 false positive caused by stored outcome yes vs
    dominant_side no, and one volume_spike_v1 noise row at exactly 5.0x with
    low_notional/thin_baseline/near_threshold caveats.
  - >-
    Directional alert persistence now prefers detected dominant_side for
    directional_cluster_v1 and momentum_v1 alert rows and suppression keys when
    available, preventing future stored-outcome/dominant-side mismatches like
    504e373a. Focused runner tests passed with tests/test_runner_suppression.py:
    27 passed.
  - >-
    Deterministic DB-gated replay proof passed on 2026-06-18:
    tests/test_replay_backtest_db.py::test_persisted_directional_alert_outcome_matches_dominant_side_audit
    seeds no-side history, replays a yes-side raw event through replay_from_db
    with persist=True and seed=True, persists a directional_cluster_v1 alert
    with alerts.outcome_key=no while evidence retains outcome_key=yes,
    directional_side=yes, and dominant_side=no, and get_directional_outcome_audit
    reports the synthetic row as status=match. The full DB-gated
    tests/test_replay_backtest_db.py file passed with 7 tests, and default
    no-DB verification skips those tests.
  - >-
    Executable local review-pass gate is implemented on 2026-06-18:
    python scripts\task.py review-pass and python -m pmfi.cli review-pass both
    passed. The gate is read-only, offline, and file-backed; it checks required
    operating docs/files, task graph posture, required local-only/Postgres/raw
    lineage/no-trading/offline-default constraints, required high-priority
    commands, milestones M0-M10, V4 review-pass docs, task-wrapper routing,
    default verification staying offline, and the latest WORKLOG verification
    plus residual-risk sections. JSON output is available with --format json.
  - >-
    DB replay window validation now fails closed on 2026-06-18:
    pmfi replay --from-db --from/--to rejects malformed, naive, future, zero
    relative, partial relative, and inverted windows before loading config or
    opening Postgres. Valid timezone-aware ISO and positive relative windows
    still pass normalized UTC datetimes to replay_from_db. Focused offline tests
    passed with tests/test_replay_cli_offline.py: 23 passed.
  - >-
    Replay report artifact generation is now executable on 2026-06-18 through
    opt-in pmfi replay --report. Fixture replay and DB replay result lists write
    timestamped, non-overwriting local ignored text reports under reports/replay
    after successful replay, while default replay remains artifact-free and
    invalid DB windows still fail before report writing, config loading, or
    Postgres access.
  - >-
    Windows task-wrapper DB replay is implemented on 2026-06-18:
    python scripts\task.py db-replay forwards to pmfi.cli replay --from-db,
    preserves artifact-free defaults, and forwards exact-window/report/persist
    operator flags without duplicating CLI DB or window validation.
  - >-
    Fresh post-fix exact bounded live/soak sample passed on 2026-06-18 for the
    completed run from 2026-06-18T16:23:02.443594+00:00 through
    2026-06-18T16:33:04.925911+00:00 with raw_events=3499,
    normalized_trades=64, alerts=0, unresolved_dead_letters=0,
    open_data_quality_incidents=0, raw_evidence_duration_minutes=9.982, and
    both required venues present: kalshi raw_events=56 normalized_trades=56
    duration_minutes=8.665; polymarket raw_events=3443 normalized_trades=8
    duration_minutes=9.982. The no-alert result is partial post-fix runtime
    evidence, not proof of new directional dominant-side persistence rows.
  - >-
    Local directional outcome audit is implemented through pmfi alerts
    outcome-audit. It is read-only, supports exact --since/--until windows and
    --strict, compares directional_cluster_v1 and momentum_v1 stored
    outcome_key rows against evidence dominant_side, and DB-smoked with the
    known pre-fix 24h cohort as checked=3, matched=2, mismatches=1, ok=false.
  - >-
    Fresh 15-minute post-fix exact bounded live/soak sample passed on
    2026-06-18 for the completed run from 2026-06-18T16:43:21.199316+00:00
    through 2026-06-18T16:58:23.677711+00:00 with raw_events=4717,
    normalized_trades=90, alerts=3, unresolved_dead_letters=0,
    open_data_quality_incidents=0, raw_evidence_duration_minutes=14.983, and
    both required venues present: kalshi raw_events=57 normalized_trades=57
    duration_minutes=13.823; polymarket raw_events=4660 normalized_trades=33
    duration_minutes=14.983. Exact outcome-audit for the run returned checked=0,
    so it remains non-directional evidence.
  - >-
    The three-alert post-fix non-directional sample was fully reviewed on
    2026-06-18: pmfi alerts fp-rate --since 30m reported Reviewed=3, FP=0,
    TP=3, Noise=0; pmfi report --since 30m reported review_queue.total=0,
    reviewed_total=3, and no unresolved dead letters or open data-quality
    incidents. The reviewed rows were two volume_spike_v1 true positives with
    post_fix_volume_spike category and low_notional/thin_baseline caveats, plus
    one large_trade_absolute_v1 true positive with payout_notional_low_capital
    caveat.
  - >-
    Fresh 30-minute post-fix exact bounded live/soak sample passed on 2026-06-18
    for the completed run from 2026-06-18T17:08:08.882160+00:00 through
    2026-06-18T17:38:11.395377+00:00 with raw_events=10328,
    normalized_trades=144, alerts=2, unresolved_dead_letters=0,
    open_data_quality_incidents=0, raw_evidence_duration_minutes=29.978, and
    both required venues present: kalshi raw_events=66 normalized_trades=66
    duration_minutes=29.263; polymarket raw_events=10262
    normalized_trades=78 duration_minutes=29.978. Exact outcome-audit for the
    run returned checked=0, and strict mode returned ok=false with exit_code=1
    because no directional or momentum rows existed, so it remains
    non-directional evidence.
  - >-
    The two-alert 30-minute post-fix sample was fully reviewed on 2026-06-18:
    pmfi alerts fp-rate --since 10m reported Reviewed=2, FP=0, TP=2, Noise=0;
    pmfi report --since 45m reported review_queue.total=0, reviewed_total=2,
    and no unresolved dead letters or open data-quality incidents. The reviewed
    rows were one market_relative_large_trade_v1 true positive with
    post_fix_market_relative_large_trade category and one volume_spike_v1 true
    positive with post_fix_volume_spike category, both from the same Kalshi
    no-side Bitcoin trade with capital_at_risk_usd=6207.60.
  next_recommended_focus:
    id: post_fix_directional_outcome_live_validation
    summary: >-
      Treat dominant-side persistence as implementation-proven by deterministic
      DB replay/audit evidence, keep natural post-fix directional live sampling
      opportunistic, and continue reviewed packet accumulation before any
      threshold changes.
  residual_proof_gaps:
  - >-
    The currently sampled live alert queue is labeled in local Postgres: older
    reviewed evidence contains 23 volume_spike_v1 noise rows, 1
    market_relative_large_trade_v1 true-positive row, and 1 fresh
    post-calibration volume_spike_v1 true-positive row; the second
    post-calibration sample adds 3 true positives, 1 directional outcome
    mismatch false positive, and 1 near-threshold volume_spike_v1 noise row; the
    latest post-fix samples add 3 non-directional true positives plus 2 more
    non-directional true positives from a 30-minute run. This is local review
    truth, not final threshold truth or remote publication truth.
  - Publication is complete for current local commits; future local changes still
    need the publish-ready gate before another push.
  - Review-packet export is implemented and DB-smoked, but generated packets are
    local ignored artifacts; packet inspection and fresh post-calibration
    runtime/review proof are now recorded; future threshold changes still need
    more reviewed post-calibration packet evidence plus replay or fresh-soak
    proof before changing rules.
  - >-
    The directional dominant-side persistence fix is covered by focused unit
    tests, was motivated by live evidence, has clean post-fix runtime samples,
    now has an exact-window outcome-audit command, and has deterministic
    DB-gated replay proof that a persisted directional_cluster_v1 row stores
    dominant_side in alerts.outcome_key when the triggering trade outcome
    differs. Natural post-fix live traffic still has not produced a fresh
    directional_cluster_v1 or momentum_v1 row, so that remains an opportunistic
    live-observation gap rather than the implementation proof source.
  - Authenticated Kalshi WebSocket remains deferred; the supported Kalshi path is
    local REST polling unless future credentials and signing are approved.
high_priority_commands:
- command: python scripts\verify.py
  why: canonical offline verification gate; no default live API calls
- command: python scripts\db_local.py up
  why: start local Docker/Postgres when DB-backed proof is needed
- command: python scripts\db_local.py init
  why: apply local schema and seed venues before first DB verification
- command: python scripts\db_local.py verify
  why: validate local Postgres schema and seeded venues after Docker is up
- command: python scripts\task.py review-pass
  why: run the local coherence/docs-drift gate without DB, live API, or artifact writes
- command: python -m pytest tests\test_replay_backtest_db.py -q
  why: DB-gated deterministic replay/persist proof when PMFI_DB_URL is set; skips in default offline verification
- command: python scripts\task.py db-replay --from <started_at> --to <ended_at> --limit 0 --report
  why: run exact-window DB replay/backtest proof with fail-closed timezone-aware bounds and write the local ignored replay report
- command: python scripts\task.py handoff --db-verify
  why: write a local-only handoff evidence snapshot with current DB readiness
- command: python scripts\task.py publish-ready --fetch
  why: validate clean branch ancestry and fresh remote-tracking evidence before any push or PR claim
- command: python scripts\task.py soak --window 2h
  why: validate persisted live-soak evidence by lookback window; use --required-venue with --min-required-venue-duration-minutes for strict venue proof
- command: python scripts\task.py soak --since <started_at> --until <ended_at>
  why: validate one exact bounded ingest run using explicit timestamps
- command: python scripts\task.py fixture-replay
  why: fixture-backed replay proof for raw-to-alert behavior
- command: python scripts\task.py health
  why: inspect local daemon heartbeat and stale/missing runtime state
- command: python scripts\task.py report --since 7d
  why: inspect local alert, review, and data-quality posture from Postgres
- command: python scripts\task.py review-packet --since 24h
  why: export a local JSON packet for reviewed alert cohorts, calibration, and handoff audit
- command: python scripts\task.py outcome-audit --since <started_at> --until <ended_at> --strict
  why: exact-window read-only audit that proves or fails directional/momentum dominant_side persistence
advancement_policy:
  default: Work lower layers before higher layers when both are feasible.
  allowed_parallel_work: true
  allowed_top_down_spikes: true
  spike_payback_required: tests_or_schema_or_fixtures_or_interfaces_or_blocker
  never_bypass:
  - local_only_scope
  - no_trading_or_order_placement
  - raw_evidence_lineage
  - default_offline_verification
  - no_secret_commits
  - no_weakened_tests
  orthogonal_decisioning_for_unclear_work: true
  talmudic_style_debate_for_nontrivial_decisions: true
  anti_ceremony_material_results_over_process: true
  orthogonal_reasoning_allowed: true
  compact_decision_debate_required_for_unclear_architecture: true
  material_progress_over_ceremony: true
  decision_loop:
    when: unclear_architectural_or_organizational_or_orchestration_or_data_semantic_choice
    format: question_strongest_case_objection_orthogonal_alternative_consensus_payback_next_check
    max_default_length: short_enough_to_not_block_implementation
milestones:
- id: M0
  name: repository verification baseline
  status: complete
  gate: python scripts\verify.py
  recommended_when: always before meaningful work
  unlocks:
  - M1
  - M2_fixture_contracts
  - CLI_spike
- id: M1
  name: local postgres proof
  status: core_proven
  gate: python scripts\db_local.py verify
  proof: Local Postgres schema and venue seed verification have passed in prior
    DB-gated runs; rerun when Docker/Postgres state changes.
  if_blocked: record Docker/Postgres blocker and advance fixture-backed repository
    contracts
  unlocks:
  - M2
- id: M2
  name: raw event persistence
  status: core_proven
  gate: raw fixture persistence test
  proof: Raw event persistence is implemented and covered by fixture/DB-backed
    replay paths; raw lineage remains a binding constraint.
  parallel_safe_without_live_feeds: true
  unlocks:
  - M3
  - M4_fixture_pipeline
- id: M3
  name: normalized trade contracts
  status: core_proven
  gate: python scripts\task.py fixture-replay
  proof: Normalization contracts are fixture-backed, with malformed payloads
    routed to dead-letter behavior instead of aborting the operator proof.
  parallel_safe_without_live_feeds: true
  unlocks:
  - M4
  - M6_basic_metrics
- id: M4
  name: fixture replay pipeline
  status: core_proven
  gate: python scripts\task.py fixture-replay
  proof: Fixture replay exercises the local raw-to-alert path without live calls.
  unlocks:
  - M6
  - CLI_spike
- id: M5
  name: opt-in live read adapters
  status: strict_polymarket_and_kalshi_soaks_proven
  gate: python scripts\task.py soak --window 2h
  proof: Strict Polymarket live soak passed on 2026-06-18 with raw_events=11643,
    normalized_trades=781, alerts=10, unresolved_dead_letters=0,
    open_data_quality_incidents=0, raw_evidence_duration_minutes=68.9; strict
    Kalshi-required soak passed on 2026-06-18 with kalshi raw_events=1144,
    kalshi normalized_trades=1144, kalshi raw_evidence_duration_minutes=60.862,
    unresolved_dead_letters=0, open_data_quality_incidents=0.
  if_blocked: improve source map, adapter interface, and fixture contracts
  unlocks:
  - live_data_quality
- id: M6
  name: rolling metrics
  status: core_proven
  gate: deterministic metric fixture tests
  proof: Rolling metric windows and baseline enrichment are covered by deterministic
    tests and DB-gated runs.
  unlocks:
  - M7
- id: M7
  name: explainable alert engine
  status: core_proven
  gate: fixture alert report
  proof: Explainable alert rules fire from fixture and replay paths with evidence
    payloads and degraded confidence handling.
  unlocks:
  - M8
  - M9
- id: M8
  name: local alert delivery
  status: core_proven
  gate: stdout/file delivery tests
  proof: Local stdout/file/localhost delivery surfaces are implemented and tested;
    external notification SaaS remains excluded.
  unlocks:
  - operator_workflow
- id: M9
  name: replay and backtesting
  status: core_proven
  gate: replay report under reports
  proof: Replay and backtest paths are implemented; pmfi replay --report writes
    timestamped local ignored reports under reports/replay after successful
    fixture or DB replay, and generated reports are not publish-readiness claims.
  unlocks:
  - M10
- id: M10
  name: local hardening and operator UX
  status: continuous_hardening
  gate: python scripts\verify.py plus local ops smoke
  proof: Operator commands, health/reporting, fail-closed validation, and Windows
    UX have substantial evidence; continue hardening as live runtime evidence grows.
  unlocks: []
parallel_tracks:
- id: CLI_spike
  purpose: clarify local operator workflow without hiding network calls
  payback: tests and docs for resulting command contract
- id: fixture_contracts
  purpose: advance without live venue access
  payback: normalizer tests and source-map blocker notes
- id: sql_review
  purpose: harden Postgres schema before Docker is available
  payback: SQL tests or db verification when possible
- id: alert_payload_examples
  purpose: clarify utility and explainability
  payback: scoring tests and replay snapshots
- id: orthogonal_decision_spike
  purpose: resolve unclear architecture, organization, orchestration, or product-utility
    choices through orthogonal lenses and compact Talmudic debate
  payback: consensus action plus test/schema/fixture/interface/local command/report/blocker
decision_policy:
  unclear_work_mode: orthogonal_problem_solving
  non_trivial_decision_method: compact_talmudic_debate
  material_results_priority: true
  canonical_doc: docs/governance/12_decision_methods.md
  debate_output_required:
  - consensus_action
  - payback_artifact
  - next_check
  avoid:
  - low_velocity_ceremony
  - documentation_without_executable_progress
  - single_frame_architecture_decisions
