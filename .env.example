POSTGRES_DB=pmfi
POSTGRES_USER=pmfi
POSTGRES_PASSWORD=pmfi_local_password_change_me
DATABASE_URL=postgresql://pmfi:pmfi_local_password_change_me@localhost:5433/pmfi

PMFI_ENABLE_LIVE=0
PMFI_POLYMARKET_API_BASE=https://clob.polymarket.com
PMFI_KALSHI_API_BASE=https://api.elections.kalshi.com/trade-api/v2
PMFI_KALSHI_API_KEY_ID=
PMFI_KALSHI_PRIVATE_KEY_PATH=

# HTTP alert delivery targets http://localhost:8765/alerts by default and is
# not configurable via environment variable. To change the endpoint, set
# HttpDelivery(endpoint=...) directly in your alert delivery configuration.
