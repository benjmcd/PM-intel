# Co-fire alert-quality artifacts — audit state note (2026-07-06)

> **UPDATE (post-merge, origin/main `4ea003e`):** most of the fixes below LANDED via PRs #98/#97. Now on main: `outcomes-cofire-reval-2026-07-06.json` has `9a357683`=noise + rule v1.2 (**tp=22**); the three `co-fire-validation-reval-*.md` were **regenerated to tp=22**; the v1.1 new-labels artifacts carry SUPERSEDED banners; the 44-cohort has an `_audit_note`; the R3 labeler was **narrowed to v1.3**; `breach-denominators-2026-07-06.md` + `cofire-losing-leg-adjudication-2026-07-06.md` (25 legs) added. **Correction:** the `31.5%` volume_spike BREACH **IS reproducible** (`28/89`, floor `this_trade_usd>=850`) — an earlier draft wrongly called it irreproducible; the non-reproducing figure was `51.6%`. **STILL OPEN (operator-gated):** DB `alert_reviews` continues to serve `9934a6e1`+`9a357683` as `tp` (re-ratification held) → a JSON-vs-DB divergence persists; momentum scope + flip-flag + R3-emit held. The per-artifact table below describes the PRE-fix state for history.

**Read this before trusting the numbers in the `*-reval-*` artifacts in this directory.** A multi-pass audit (2 Codex + several Opus/mixed verification workflows, all read-only) found that several artifacts here are **stale** and that two labels are **wrong**. No fixes have been applied yet (operator direction: finalize the record first). The canonical, self-contained record is **`plans/2026-07-06-state-and-roadmap.md` §12–§15**; this note is a directory-local pointer so the stale files are not read at face value.

## Corrected ground truth (v1.2)
- The enlarged co-fire re-validation cohort (`outcomes-cofire-reval-2026-07-06.json`) has **two** post-close-fire settlement-only `tp` that must be `noise` under LABELING_RULE v1.2: **`9934a6e1`** (fixed in PR #95) and **`9a357683`** (MISSED by #95 — still `tp`). The correct v1.2 count is **tp=22**, not 23 or 24.
- `9a357683` = `directional_cluster_v1`, market `KXWCSCORE-26JUN18CANQAT-CAN6QAT0`, fired ~38 min AFTER its live `facts.close_time`; still `tp` in this cohort JSON, in `outcomes-2026-07-02.json`, and in DB `alert_reviews`. UNFIXED.

## Per-artifact status
| Artifact | Status |
|---|---|
| `co-fire-reval-report-2026-07-06.md` | **CORRECTED** (v1.2, tp=23, honest framing) — but predates the `9a357683` finding (true count is tp=22). |
| `co-fire-validation-reval-2026-07-06.md` (+ `-w300`, `-w1800`) | **STALE v1.1** — show `tp=24` / `tp_leg_visible=24/24`. Machine-generated by `validate_cofire.py`; regenerate under v1.2 (→ tp=22) or treat as pre-v1.2. |
| `cofire-reval-new-labels-2026-07-06.md` | **STALE v1.1** — header `v1.1`, lists `9934a6e1` as `tp`. Superseded by #95 + this note. |
| `outcomes-cofire-reval-new-2026-07-06.json` | **STALE v1.1** — top-level `rule=v1.1`, `9934a6e1=tp`. |
| `outcomes-cofire-reval-2026-07-06.json` | Records are v1.2 (`9934a6e1=noise`) but **top-level `rule` metadata still says `v1.1`**, and `9a357683` is still `tp` (should be `noise`). |
| `outcomes-2026-07-02.json` | The immutable v1.1 44-cohort snapshot — embeds `9934a6e1`+`9a357683` as `tp`; these propagate into the 110-cohort via "existing labels win" (contamination — §14). |
| `co-fire-validation-2026-07-06.md` | The frozen 44-cohort validation report (`tp=16`, `tp_leg_visible=16/16`). Its `tp=16` **includes** both mislabels (`9934a6e1`, `9a357683`); correct-under-v1.2 the 44-cohort tp is 14. Gate verdict PASS is unaffected (removing tp cannot cause tp-loss). |

## Other recorded findings (full detail in plan §12–§15)
- **`review-packet --group-cofire` reduction is 0 unconditionally** (`src/pmfi/commands/alerts.py:1319-1326`+`:1244`, `review_state` defaults `'reviewed'`) — a real bug found by running the view; no CLI surface exposes a non-zero co-fire reduction count.
- **81 DB `tp` vs R3 = a lane mismatch, NOT a defect** (§15): R3 (`cross_market_hedge`) is a **non-authoritative proposal** in `fetch_outcomes.py`; ADR-0009 adopts grouping but declines a hedge label. Only 15/81 rest on settlement survivorship; 66/81 on fire-time flow/magnitude evidence. Recommendation = HYBRID (narrow R3, document, adjudicate a few losing legs — no mass relabel). KXBTCD is a directional ladder R3 would wrongly fp.
- **`volume_spike` "31.5%" BREACH figure is not reproducible** on the live DB (denominator undocumented; live 51.6%/55.8%); `directional_cluster` 19.7% reproduces; `momentum_v1` (11.1%) shares the same root cause.
- **Assumptions that do NOT hold:** "900s generalizes" (MEXKOR fragments across a 1676s gap), "reval-set complete" (curated; 81 DB tp unexamined vs R3), "only 2 post-close tp" (true cohort-locally, unverified DB-wide).
- **Confirmed clean:** gates reproduce (verify 1393/94, validate 44=16/16 + reval=23/23, db PASS); emission fence clean; multi-leg 4-event set complete (triple-method); exactly two cohort mislabels via exhaustive symmetric v1.2 re-derivation.

## Deferred fixes (awaiting operator go — see plan §12/§14/§15)
Relabel `9a357683` (+ decide 44-cohort); regenerate stale v1.2 artifacts (tp=22); fix the review-packet reduction bug; re-derive BREACH denominators; narrow R3 (v1.3) + adjudicate the losing-leg subset; `momentum_v1` scoping; DB re-ratification (ratification-gated). **No DB writes, no mass relabel, and no `src/` changes have been made.**
