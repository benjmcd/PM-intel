# Continuation prompt

Read `AGENTS.md`, `FAST_ADVANCE.md`, `AGENT_START_HERE.md`, `WORKLOG.md`, and the active plan. Run `python scripts\verify.py` if the environment is ready, then `python scripts\task.py status`.

Continue from the highest-leverage verified next slice, not necessarily the first unchecked milestone. Trust repository files and tests over memory. Update `WORKLOG.md` before handoff.
