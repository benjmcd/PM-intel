# Initial Codex prompt

Read `AGENTS.md`, `AGENT_START_HERE.md`, `FAST_ADVANCE.md`, and the latest `WORKLOG.md` entry. Then run:

```powershell
python scripts\verify.py
python scripts\task.py status
```

Open the active plan, adaptive milestone map, architecture docs, or testing docs only as needed for the slice you choose. Append a reconnaissance note to `WORKLOG.md` with files inspected, checks run, missing functionality, assumptions/blockers, and the next highest-leverage slice.

Advance the repo as quickly as feasible toward a usable local product. Use bottom-up sequencing as the default, but use bounded top-down spikes or parallel fixture-backed work when they accelerate verified progress. Do not rely on chat history. Do not weaken checks, bypass raw evidence lineage, or cross local-only exclusions.
