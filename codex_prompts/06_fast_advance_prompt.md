# Fast advance Codex prompt

Read `AGENTS.md`, `FAST_ADVANCE.md`, `AGENT_START_HERE.md`, and `docs/implementation/06_adaptive_milestone_map.md`. Run `python scripts\verify.py` if the environment is ready. Run `python scripts\task.py status`.

Advance the repo as quickly as feasible toward a fully usable local product. Use bottom-up sequencing as the default, but do not let stale process language or unchecked milestone order block obvious productive work. If a bounded top-down spike is the fastest way to clarify missing contracts or product utility, do it, then convert the learning into tests, schema, fixtures, interfaces, or docs.

Keep local-only scope, no trading/order placement, no SaaS/hosted/platform work, no secrets in repo, no live calls in default verification, and no weakened tests. Update `WORKLOG.md` with changed files, checks run, blocker status, and the next highest-leverage step.


When architecture, orchestration, data semantics, or next-slice selection is unclear, use an orthogonal approach and a compact Talmudic-style debate. Record only the consensus and executable validation target; do not create ceremony.


For unclear architecture, organization, orchestration, or product-utility decisions, use the orthogonal problem-solving lenses in `FAST_ADVANCE.md`. For non-trivial decisions, use the compact Talmudic-style debate format to reach a coherent consensus, then implement the highest-leverage material next action. Avoid low-velocity ceremonial procedure when a verified local slice, test, fixture, schema/interface change, report, or precise blocker would advance the repo faster.


When debating unclear choices, keep it brief. Material local results and executable evidence outrank ceremonial procedure.


Decision method reference: `docs/governance/12_decision_methods.md`.
