model = "gpt-5.5"
sandbox_mode = "workspace-write"
approval_policy = "on-request"
approvals_reviewer = "user"

[profiles.fast]
model = "gpt-5.4-mini"
sandbox_mode = "workspace-write"
approval_policy = "on-request"

[profiles.readonly]
model = "gpt-5.4-mini"
sandbox_mode = "read-only"
approval_policy = "on-request"
