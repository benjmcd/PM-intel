# pmfi.commands — domain-grouped CLI command handlers
# cli.py retains parser construction, dispatch, and functions whose test patches
# target pmfi.cli.* attributes (asyncio.run, ROOT, _cmd_baselines_*, etc.).
