# PMFI Fast Advance Context

## Task Statement
Advance the PMFI repo quickly and safely from the supplied handoff, using subagents for implementation where useful, while preserving local-only/no-trading scope and avoiding deletes.

## Desired Outcome
- Choose the highest-ROI local-only slice from current repo evidence.
- Implement a narrow verified improvement with tests.
- Keep verification passing and update durable handoff state.

## Known Facts And Evidence
- Handoff authority: `C:\Users\benny\Downloads\PM-intel_handoff.md`.
- Current worktree authority wins over handoff claims.
- `omx` CLI is not available on PATH, so native Codex goal state is used for this run.
- Initial global-Python verify failed because dependencies were missing.
- Local `.venv` was created and installed with `.[dev]`.
- Venv `scripts\verify.py` passed after fixing repo verification hygiene.
- Baseline pass: `196 passed, 11 skipped`.

## Constraints
- Local-only Windows tool; no SaaS, hosted runtime, billing, auth, external secret manager, or trading/order placement.
- No file deletion/removal. Archive only if removal becomes necessary.
- Prefer narrow tests and source changes over ceremonial docs.
- Raw evidence before derived records; fixture-first/default-offline verification.

## Unknowns
- Local Postgres availability is unconfirmed in this session.
- Live venue proof remains opt-in and unverified here.
- Full `omx ultragoal` ledger cannot be used until `omx` is installed or on PATH.

## Likely Touchpoints
- `src/pmfi/normalization.py`
- `tests/test_normalization_edge_cases.py`
- `src/pmfi/config.py`
- `src/pmfi/cli.py`
- `src/pmfi/delivery/server.py`
- `tests/test_config.py`
- `tests/test_cli.py`
- `tests/test_delivery.py`
- `WORKLOG.md`
